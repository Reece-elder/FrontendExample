// 'use strict'

// Used to write secure JavaScript, changes existing 'Bad Syntax' to errors
// Eliminates *some of* the silent errors in JS Code

// CUsing a variable without declaring it

test = "this won't be printed";
console.log(test);

// Deleting a variable, object, function
// Duplicaing a paramter name
// Writing to read only or get only properties

// Certain words that cannot be used for variables:
let eval, Arguments, With, this, implements, interface, let, package, private, protected, public, static, yield;

//Just use common sense when naming variables!